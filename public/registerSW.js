// Legacy filename kept so any cached HTML that still references
// /pickle-pair/registerSW.js doesn't 404. Does nothing — the real cleanup
// lives in sw.js, which the old SW will pick up as an update automatically.
